analyzer:
  exclude:
    - build/**
    - android/**
include: package:flutter_lints/flutter.yaml

linter:
  rules:
    # Les champs privés ne peuvent pas être des paramètres nommés en Dart,
    # donc la règle ne s'applique pas à nos constructeurs.
    prefer_initializing_formals: false